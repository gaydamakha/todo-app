{% block todolist %}
    <ul style="list-style-type: none">
        {% for todo in todos %}
            <li class="a-task">
                <a href="/todos/{{ todo.id }}">
                    {{ todo.title }}
                </a></li>
        {% endfor %}
    </ul>
{% endblock %}